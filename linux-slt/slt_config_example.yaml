# SLT下位机系统配置文件示例
# 格式: INI风格 (section/key: value)

[serial]
port: /dev/ttyAMA0
baud_rate: 115200

[network]
# mode: tcp 启用TCP通信模式, serial 使用串口
mode: tcp
addr: 0.0.0.0
port: 9999

[logging]
directory: /var/log/slt/

[commands]
default_timeout_ms: 5000
max_command_size: 4096
